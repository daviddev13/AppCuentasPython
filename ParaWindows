# Instalar PyInstaller
pip install pyinstaller

# Crear ejecutable
pyinstaller --onefile --windowed --name="MiAplicacion" main.py


paso 2
 Edita el archivo MiAplicacion.spec que se creó:
python# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('app1', 'app1'),
        ('app2', 'app2'),
        ('comunes', 'comunes'),
    ],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pex = PEX(a)
exe = EXE(
    pex,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='MiAplicacion',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,  # False para Tkinter sin consola
    disable_windowed_traceback=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='icono.ico'  # Si tienes un icono
)


Paso 3: Compila usando el spec:
bashpyinstaller MiAplicacion.spec


Opciones útiles:

--onefile: Crea un único archivo .exe
--windowed o --noconsole: Oculta la consola (ideal para apps con GUI)
--icon=icono.ico: Añade un icono personalizado
--name=NombreApp: Define el nombre del ejecutable

El .exe estará en la carpeta dist/


\\
